```{r}
library(tidyverse)
library(datasets.load)
```

```{r}
desc <- read_csv("descriptions.csv")
```


```{r}
desc
```

sample a few

```{r}
desc %>% 
  select(dataset, technique, note) %>% 
  separate_rows(technique, sep = ",") %>% 
  filter(str_detect(technique, "categorical")) %>% # this line to select a technique
  slice_sample(n = 10)
```

